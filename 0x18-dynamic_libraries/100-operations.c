/**
 * add - get the sum of two number
 * @x: input number
 * @y: input nuber
 * Return: result the sum of two number
 */
int add(int x, int y)
{
	return (x + y);
}
/**
 * sub - get the diff between two numbers
 * @x: input argument
 * @y: input argument
 * Return: result of subtract two numbers
 */
int sub(int x, int y)
{
	return (x - y);
}
/**
 * mod - get mod
 * @x: input arg
 * @y: input arg
 * Return: mod of two numbers
 */
int mod(int x, int y)
{
	return (x % y);
}
/**
 * mul - multibly
 * @x: input arg
 * @y: input arg
 * Return: mul result
 */
int mul(int x, int y)
{
	return (x * y);
}
/**
 * div - division
 * @x: input arg
 * @y: input arg
 * Return: divition of two numbers
 */
int div(int x, int y)
{
	return (x / y);
}
