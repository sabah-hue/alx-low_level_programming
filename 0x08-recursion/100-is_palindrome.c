#include "main.h"

/**
 * int is_palindrome -  check a string is a palindrome.
 *
 * Description: a function that returns 1 if a string is a palindrome and 0 if not.
 * @s: input string
 *
 * Return: 1 if a string a palindrome and 0 if not.
 */
int is_palindrome(char *s)
{
}
