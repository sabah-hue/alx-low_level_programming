#include "main.h"

/**
 * _strlen - string length
 *
 * Description: a function that returns the length of a string.
 * @s: input parameter
 *
 * Return: nothing
 */
int _strlen(char *s)
{
	strlen(*s);
}
