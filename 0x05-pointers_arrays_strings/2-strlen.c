#include "main.h"

/**
 * _strlen - string length
 *
 * Description: a function that returns the length of a string.
 * @s: input parameter
 *
 * Return: length
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
