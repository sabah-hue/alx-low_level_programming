#include <stdio.h>
/**
 * main - entry point , no inputs needed
 *
 * Description:  A function that print numbers from 0 to 9
 *
 * Return: 0 in success case
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
	{
		printf("%d", i);
	}
	return (0);
}
